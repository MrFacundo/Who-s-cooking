<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= image_path 'restaurant_banner.jpg' %>); text-align: center;">
  <div class="container">
    <h1><%= @restaurant.name %></h1>
    <h2><%= @restaurant.description %></h2>
  </div>
</div>

<div class="container">
  <div>
    <div class="row justify-content-center mt-5 mb-5">
      <h1>Our Philosophy</h1>
    </div>
    <div class="row justify-content-center">
      <div class="col-8 text-justify mb-5">
        <p>Our chefs have worked their way around the world. They have gained experience in Asia, Australia, Dubai, Kasachstan and Switzerland, to name but a few. They have been taught by michelin star chefs and are always ready for something new. They are two perfectionists, who are driven by their passion for cooking and authentic dishes. Their creativity knows no limits. </p>
      </div>
    </div>
  </div>
</div>

<div class="container">
  <div class="row justify-content-center">
    <h1>Our Home Cooking Experiences</h1>
  </div>
  <div class="row justify-content-center">
    <div class="col-8">
      <% @restaurant.meals.each do |meal| %>
        <div class="card mb-3 mt-3">
          <%= image_tag "meal_placeholder.jpg" %>
          <div class="card-footer">
            <div class="text-left" class="meal-difficulty">
              <ul class="list-inline mt-0">
                <% meal.difficulty.times do %>
                  <li class="list-inline-item"><i class="fas fa-utensil-spoon"></i></li>
                <% end %>
                <% (5-meal.difficulty).times do %>
                  <li class="list-inline-item inactive"><i class="fas fa-utensil-spoon"></i></li>
                <% end %>
              </ul>
            </div>
            <div class="meal-description text-left mt-3">
              <h3><%= meal.name %></h3>
              <p><%= meal.description %></p>
            </div>


<%# MealCategory.find_by(id:11).category.name %>
            <div class="text-right">
              <ul class="list-inline mb-0">
                <% meal.categories.all.each do |c| %>
                  <% if c.name == "Italian" %>
                    <li class="list-inline-item"> It worked! </li>
                  <% end %>
                <% end %>
              </ul>
            </div>


            <div class="text-right">
              <ul class="list-inline mb-0">
                <li class="list-inline-item"><i class="fas fa-carrot"></i></li>
                <li class="list-inline-item"><i class="fas fa-bread-slice"></i></li>
                <li class="list-inline-item"><i class="fas fa-egg"></i></li>
              </ul>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
