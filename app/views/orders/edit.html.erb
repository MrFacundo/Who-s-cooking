<div class="container">
  <div class="shopping-cart">
    <div class="text-center">
      <h1><i class="fas fa-shopping-cart"></i> Shopping Cart</h1>
      <h2><%= @order.restaurant.name %></h2>
    </div>

    <% @order.order_items.each do |oi| %>
      <div class="row justify-content-around">
        <p><%= oi.meal.name %></p>
        <p><%= oi.quantity %></p>
        <%= link_to "<i class='fas fa-times'>Remove</i>".html_safe, order_item_path(oi.id), method: :delete %>
      </div>
    <% end %>


    <div class="form">
      <%= simple_form_for [ @order ] do |f| %>
        <div class= "form-group">
          <%= f.input :delivery_time, class: "form-control" %>
          <%= f.input :delivery_address, class: "form-control" %>
          <%= f.submit "Submit Order", class: "form-control btn btn-info" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
